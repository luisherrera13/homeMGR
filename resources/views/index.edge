@layout('layouts.main')

@section('title')
houseKeeper - Encuentra todos tus objetos
@endsection


@section('content')
@loggedIn

@if(homes.length>0)
@each(homeValues in homes)
  @if(homeValues.user_id==auth.user.id)
  <div class="link-container">
    <a href="/spaces/{{ homeValues.id }}" class="link">
      <div class="link-info">
        <h3> Casa: </h3>
        <h4>{{ homeValues.home_name }}</h4>
      </div>
    </a>
    <div>
  @endif
  @endeach
@endif

@endloggedIn
@endsection
